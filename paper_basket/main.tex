% ==============================================================
% Basket-Aware Apriori-Window — 論文ドラフト（日本語版）
% ==============================================================
% コンパイル方法:
%   xelatex main.tex && bibtex main && xelatex main.tex && xelatex main.tex
% 注意: 日本語を含むため XeLaTeX + xeCJK が必要
%   macOS の場合は "Hiragino Mincho ProN" フォントを使用
% ==============================================================
\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

% --- 日本語対応 ---
\usepackage{xeCJK}
\setCJKmainfont{Hiragino Mincho ProN}
\setCJKsansfont{Hiragino Sans}
\setCJKmonofont{Hiragino Sans}

% --- 標準パッケージ ---
\usepackage{cite}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{graphicx}
\usepackage{textcomp}
\usepackage{xcolor}
\usepackage{threeparttable}
\usepackage{subcaption}
\usepackage{booktabs}

% --- 定理環境 ---
\newtheorem{definition}{定義}
\newtheorem{theorem}{定理}
\newtheorem{proposition}{命題}
\newtheorem{lemma}{補題}
\newtheorem{example}{例}

\def\BibTeX{{\rm B\kern-.05em{\sc i\kern-.025em b}\kern-.08em
    T\kern-.1667em\lower.7ex\hbox{E}\kern-.125emX}}

\begin{document}

\title{バスケット構造対応 Apriori-Window による偽共起パターンの排除}

\author{
  \IEEEauthorblockN{（執筆者未定）}\\
  \IEEEauthorblockA{ドラフト — 日本語版}
}

\maketitle
\pagestyle{plain}
\pagenumbering{arabic}

\begin{abstract}
既存の頻出パターンマイニングは主にグローバル頻度に依存するため，特定期間に集中して現れる局所的な密集パターンを十分に捉えにくい．
また，Gap 系手法は最大ギャップ閾値への感度が高く，パラメータ設定に結果が左右される．
この課題に対して DSAA2025 では，局所的な出現頻度の変動を厳密に捉えるApriori-window が提案され，最大ギャップ閾値に依存しない密集パターン抽出を実現した．
しかし同手法は，1 トランザクションを 1 アイテムセットとして扱う前提を置いており，同一時刻に複数の独立した購買イベント（バスケット）が含まれるデータでは，
バスケット間のアイテムが誤って共起として数えられる偽共起が発生する．
本稿はこの未解決課題に対して，DSAA2025 の枠組みを維持したままバスケット構造を扱える拡張手法（Basket-aware Apriori-Window, Phase 1）を提案する．
提案手法は，共起判定をトランザクション内からバスケット内へ置き換えることで，
偽共起パターン率（SPR: Spurious Pattern Rate）を原理的に 0 に抑える．
さらに，バスケット内共起においても Apriori の反単調性が成立することを示し，
従来と同様の候補枝刈りが可能であることを理論的に保証する．
合成データ実験では，バスケット数パラメータ $\lambda_{\text{baskets}}$ の増加に伴い
従来法の過検出が急増し（A1: observed spurious の平均 1.0 $\rightarrow$ 379.0），
カテゴリ数の増加で構造的な偽共起率が低下する傾向（A2: GT SPR の平均 0.827 $\rightarrow$ 0.638）を確認した．
さらにスケーラビリティ評価（A4）では，$10^6$ トランザクションで
提案手法 95.5 秒，従来法 143.6 秒（1.50 倍）となり，大規模条件での優位性を示した．
\end{abstract}

\begin{IEEEkeywords}
frequent pattern mining, spurious co-occurrence, basket structure,
sliding window, dense itemset
\end{IEEEkeywords}

\input{sec/01_intro}
\input{sec/02_related_work}
\input{sec/03_problem_definition}
\input{sec/04_proposed_method}
\input{sec/05_experiments}
\input{sec/06_conclusion}
\appendices
\input{sec/07_supplement}

% --- 参考文献 ---
\bibliographystyle{IEEEtran.bst}
\bibliography{refs}

\end{document}
