% ==============================================================
\section{提案手法}\label{sec:method}
% ==============================================================

本節では\textbf{イベント帰属パイプライン}の5ステップを詳述する．
Phase 1（Apriori-window）で得られたパターン集合 $\mathcal{F}$ と
その密集区間，および外部イベント集合 $\mathcal{E}$ を入力として，
統計的に有意なパターン--イベント帰属の集合を出力する．

\paragraph{対象パターン}
頻出パターンマイニングの本質はアイテム間の\textbf{共起構造}の発見にある．
単一アイテム（$|P| = 1$）の頻度変動は通常の時系列分析で十分に捕捉可能であり，
パターンマイニングに基づく帰属パイプラインの分析対象としては冗長である．
したがって，本パイプラインは共起パターン（$|P| \geq 2$）のサポート変動のみを
帰属対象とする．
Step~1--4 はパターン長に依存しない一般的な構造を持ち，
Step~5（重複排除）では，パターン長 $l$ に応じた同一長過半数共有と
異長間の部分集合排除の2段階で冗長パターンを除去する（\ref{sec:dedup}節で詳述）．
図~\ref{fig:pipeline}にパイプラインの概要と帰属の具体例を示す．
帰属スコアリングと統計検定の手順をアルゴリズム~\ref{alg:scoring}に，
重複排除の手順をアルゴリズム~\ref{alg:dedup}に示す．

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{fig/pipeline_overview.pdf}
\caption{(a) パイプラインの全体構成．入力（パターン集合，密集区間，イベント）から5ステップを経て有意な帰属を出力する．(b) 帰属スコアリングの例示．サポート時系列上の変化点 $\tau_{\uparrow}, \tau_{\downarrow}$ とイベント区間の関係から，近接度（prox）と変化量（mag）を算出する．}
\label{fig:pipeline}
\end{figure}

\input{alg/event_attribution_pipeline}

\subsection{Step 1: 密集区間からの変化点抽出}

Phase 1（Apriori-window）は各共起パターン $P \in \mathcal{F}$（$|P| \geq 2$）に対して，
サポートが閾値 $\theta$ 以上となるウィンドウ左端の連続区間（密集区間）
$\{[s_1, e_1], [s_2, e_2], \ldots\}$ を既に出力している．
本パイプラインではサポート時系列全体を再計算せず，これらの密集区間を直接利用する．
各密集区間 $[s_i, e_i]$ から上昇変化点 $s_i$ と下降変化点 $e_i + 1$（存在する場合）を抽出し，
変化量（式~(\ref{eq:magnitude})）の計算に必要な局所サポートのみを，
パターン出現リスト $T_P$ 上の二分探索により $O(\log |T_P|)$ で求める．

\subsection{Step 2: 変化量の評価}

Step 1 で密集区間の端点から変化点が既に抽出されているため，
本ステップではサポート時系列全体を走査する必要はない．
本手法が帰属対象とするのは
「密集区間の出現・消失に対応する切替点」であり，
密状態の内部で起こる任意のサポート変動ではない．

各変化点 $\tau$ に対して，変化量を交差前後の窓平均の差（レベルシフト量）として計算する:
\begin{equation}
\text{magnitude}(\tau) = |\bar{s}^{+}(\tau) - \bar{s}^{-}(\tau)|
\label{eq:magnitude}
\end{equation}
ここで $\bar{s}^{+}(\tau), \bar{s}^{-}(\tau)$ は定義~\ref{def:change_point} の局所平均であり，
$h$ はレベルウィンドウ幅（デフォルト $h = 20$）である．
この方式は瞬時差分（$s_P(\tau) - s_P(\tau-1)$）と比較して，
一過性の変動ではなく持続的な変化を捉える点で頑健性が高い．
すなわち，本手法では\textbf{検出位置}は閾値交差で決め，
\textbf{変化の強さ}は交差前後のレベル差で評価する．
閾値交差は汎用変化点検出器（CUSUM, PELT等）と比べて検出能力の最適性を主張するものではなく，
Phase 1 の密集区間出力と構造的に整合し，
サポート時系列全体の再計算（$O(|\mathcal{F}| \times N)$）を回避できる点で採用している．
予備実験では閾値交差とCUSUMが経験的に同等の検出性能を示すことを確認した．

\textbf{フィルタリング}: 有意性検定前の仮説空間削減のため，2つのフィルタを適用する:
\begin{itemize}
\item \emph{サポート振幅フィルタ}: $\max(s_P) - \min(s_P) < \delta$ のパターンを除外する．
ここで $\delta$ はユーザ指定の最小サポート振幅である．
このフィルタは時間的変動の乏しいパターンを排除し，仮説数を大幅に削減する．
\item \emph{変化量フィルタ}: 変化量が閾値未満の変化点を除外する．
\end{itemize}

\subsection{Step 3: 帰属スコアリング}

パターン $P$ の変化点 $\tau$ と各イベント $e \in \mathcal{E}$ に対して，
時間的関連を定量化する\emph{帰属スコア}を計算する．
本フレームワークはスコア関数 $A(P, \tau, e)$ の具体的な形状に依存しない
一般的な構造を持つ（Step 4 の置換検定が最終判定を行うため）．
本稿では最小限のインスタンスとして以下を採用する:
\begin{equation}
A(P, \tau, e) = \text{prox}(\tau, e) \cdot \text{mag}(\tau)
\label{eq:attribution_score}
\end{equation}

2つの構成要素は以下の通りである．

\textbf{近接度}: 指数カーネルによる時間的近さの指標:
\begin{equation}
\text{prox}(\tau, e) = \exp\left(-\frac{d(\tau, e)}{\sigma}\right)
\label{eq:proximity}
\end{equation}
ここで $d(\tau, e) = \min(|\tau - t_s|, |\tau - t_e|)$ は
変化点からイベント境界までの最小距離，$\sigma > 0$ は減衰パラメータである．

\textbf{変化量}: 式~(\ref{eq:magnitude})のレベルシフト量．
デフォルトでは絶対変化量 $|\bar{s}^{+} - \bar{s}^{-}|$ を使用するが，
アイテム頻度が非一様（Zipf分布等）な場合には，高頻度アイテムペアが
大きな絶対変化量を生じるためノイズ仮説が増加する．
この問題を緩和するため，平方根正規化を導入する:
\begin{equation}
\text{mag}_{\sqrt{}}(\tau) = \frac{|\bar{s}^{+}(\tau) - \bar{s}^{-}(\tau)|}{\sqrt{\max(1,\; \bar{s}^{-}(\tau))}}
\label{eq:mag_sqrt}
\end{equation}
完全正規化（$\div \bar{s}^{-}$）は高頻度パターンの過剰抑制を招くが，
平方根正規化は高ベースラインパターンの影響を抑えつつ
一様分布データでの精度を維持するバランスの取れた選択肢である（EX4で検証）．

パターン $P$ のイベント $e$ に対する観測スコアは，
全変化点にわたる帰属スコアの総和
$A_{\text{obs}}(P, e) = \sum_{\tau} A(P, \tau, e)$ とする．

\paragraph{設計根拠}
帰属スコア $A$ は帰属候補の\textbf{ランキング用指標}であり，
帰属判定そのものではない．
最終的な帰属の有意性は Step 4 の置換検定で判定される．
すなわち本パイプラインは，
スコアによる候補のランキングと置換検定による統計的判定の
\textbf{二層構造}を採る．
この設計により，スコア関数の具体的な形状への依存が軽減される---
スコアが真の帰属と偽の帰属を粗く弁別できれば，
置換検定の帰無分布がFDR制御の下で最終判定を行う．

乗算構成 $\text{prox} \times \text{mag}$ は，
いずれかの構成要素が0に近ければスコア全体が0に近づく
排他的フィルタとして機能する点で，
加算構成よりも偽陽性の抑制に有利である．
EX2（\ref{sec:exp_ex2}節）のアブレーション実験では，
proximity と magnitude の両方が帰属精度に不可欠であり
相補的に機能することを実証する．
なお，帰属スコアを尤度比や回帰係数に置き換えても
置換検定の枠組み内で同様に検定可能であり，
フレームワークの一般性は保たれる．

\subsection{Step 4: 置換検定とグローバルFDR補正}

\subsubsection{円形シフト帰無分布}

パターン $P$ の変化点とイベント $e$ の時間的関連が統計的に有意かを検定するため，
円形シフト置換検定を用いる．
帰無仮説 $H_0$:「変化点の位置はイベント時刻と無関係である」のもと，
$B$ 回の置換を実行する．
各置換 $b$ では，ランダムなオフセット $\delta_b \sim \text{Uniform}\{1, \ldots, T-1\}$
だけ全イベントの時刻を一斉にずらす（円形シフト）:
\begin{equation}
e_b = (\textit{id}, \textit{name}, (t_s + \delta_b) \bmod T, (t_s + \delta_b + \Delta) \bmod T)
\end{equation}
ここで $\Delta = t_e - t_s$ はイベント持続時間，$T$ は時系列長である．

各置換に対してスコア $A_{\text{perm}}^{(b)}(P, e)$ を再計算し，
未補正 $p$ 値を以下で求める:
\begin{equation}
p_{\text{raw}}(P, e) = \frac{1 + |\{b : A_{\text{perm}}^{(b)}(P, e) \geq A_{\text{obs}}(P, e)\}|}{B + 1}
\label{eq:pvalue}
\end{equation}

\begin{proposition}[置換検定の正当性]\label{prop:perm_exact}
帰無仮説 $H_0$ のもとでは，観測スコア $A_{\text{obs}}$ と
$B$ 個の置換スコア $A_{\text{perm}}^{(1)}, \ldots, A_{\text{perm}}^{(B)}$ は交換可能（exchangeable）である．
したがって式~(\ref{eq:pvalue})の $p$ 値は
$\{1/(B+1), 2/(B+1), \ldots, 1\}$ 上の離散一様分布に従い，
任意の水準 $\alpha$ で第一種過誤率が $\alpha$ 以下に制御される~\cite{davison1997bootstrap}．
\end{proposition}

\subsubsection{グローバル Benjamini--Hochberg 補正}

$M = |\mathcal{F}'| \times |\mathcal{E}|$ 個の仮説を同時に検定するため，
全パターン・全イベントにわたり Benjamini--Hochberg (BH)
手続き~\cite{benjamini1995controlling}をグローバルに適用する．

未補正 $p$ 値を昇順にソートし $p_{(1)} \leq p_{(2)} \leq \cdots \leq p_{(M)}$ とする．
Step-down 調整済み $p$ 値を以下で計算する:
\begin{equation}
\tilde{p}_{(i)} = \min\!\left(\tilde{p}_{(i+1)},\; \min\!\left(1,\; p_{(i)} \cdot \frac{M}{i}\right)\right)
\label{eq:bh}
\end{equation}
ただし $\tilde{p}_{(M)} = \min(1, p_{(M)})$ とする．
$\tilde{p}_{(i)} < \alpha$ の仮説を棄却（帰属が有意）と判定する．

BH手続きは偽発見率（FDR）を水準 $\alpha$ 以下に保ちつつ，
Bonferroni補正と比べてより多くの真の帰属を検出できる（検出力が高い）．

\begin{remark}[PRDS 条件の充足]\label{rem:prds}
BH手続きが独立でない検定統計量に対してもFDRを制御するには，
$p$ 値が部分集合上の正回帰依存性（PRDS）条件を満たす必要がある~\cite{benjamini2001control}．
本稿ではPRDS条件の形式的証明は与えない．
BH手続き適用の実務的妥当性は，以下の正の依存構造に関する
標準的直観に基づく~\cite{benjamini2001control}:
(i) アイテムを共有するパターン対のサポート時系列は正の相関を持ち，
対応する $p$ 値間に正の依存性が誘導される，
(ii) 円形シフトは全イベントを同一オフセットで一斉にずらすため，
同一パターンに対する異なるイベントの $p$ 値も同じ置換標本を共有する．
\end{remark}

\subsection{Step 5: アイテム重複排除}\label{sec:dedup}

\input{alg/dedup_pipeline}

\subsubsection{副次パターン問題}

外部イベントがアイテム $i$ の出現頻度を増加させると，
$i$ を含む\emph{全て}の共起パターンのサポートが相関して増加する．
例えば，イベントが $\{5, 15\}$ をブーストし真のパターンが $(5, 15)$ の場合，
$(5, 42)$, $(15, 78)$, $(5, 133)$ などの副次パターンも
共有アイテム経由でサポートが連動変動するため有意性検定を通過する．

この問題は統計検定だけでは解消できない．
副次パターンも共有アイテム経由で実際にサポートが変動しているため，
置換検定でも有意と判定されてしまうからである．

\subsubsection{Union-Find クラスタリングによる重複排除}

この問題を以下の後処理ステップで解決する:

\begin{enumerate}
\item 各イベント $e$ について，$e$ に有意に帰属された全パターンを収集し，パターン長 $l$ ごとにグループ化する．
\item 各長さ $l$ のグループ内で\emph{過半数共有グラフ}を構築する:
パターンをノードとし，共有アイテム数 $|P_i \cap P_j| \geq \lceil l/2 \rceil$ のパターン間にエッジを張る．
\item Union-Find（素集合）データ構造で連結成分を求める．
\item 各連結成分内で帰属スコア $A_{\text{obs}}(P, e)$ が最大のパターンのみを残す．
\item \textbf{異長パターン間の部分集合排除}: 上記の同一長排除後，イベント $e$ への帰属パターン全体に対し，$P_i \subseteq P_j$ の関係にあるペアを Union-Find で統合し，各成分内でスコア最大のパターンのみを残す．
\end{enumerate}

\begin{proposition}[重複排除の正当性（一般化）]\label{prop:dedup}
以下の簡約的な独立出現モデルを共起パターン（$l \geq 2$）に対して仮定する:
各アイテムの出現はトランザクション間で独立であり，
イベントの影響はブースト確率 $\beta$ の加算として作用する．
長さ $l$ の真のパターン $P^* = \{i_1, \ldots, i_l\}$ の全アイテムが
イベント $e$ によりブースト確率 $\beta$ でサポートを増加させられているとする．
同じ長さ $l$ の副次パターン $P'$ が $P^*$ と $s$ 個のアイテムを共有し
（$1 \leq s < l$），残り $l - s$ 個は非ブーストアイテム
（ベースライン出現確率 $p_{\text{base}}$）であるとする．
$\beta > 0$ であれば $\text{mag}(\tau, P^*) > \text{mag}(\tau, P')$ が成立する．
さらに $s \geq \lceil l/2 \rceil$ のとき $P^*$ と $P'$ は過半数共有グラフにおいて
同一連結成分に属するため，$P^*$ が保持され $P'$ は除去される．
\end{proposition}

\begin{proof}[証明の概略]
独立出現仮定のもとでは，イベント $e$ の影響下で
$P^*$ の期待サポートは $(p_{\text{base}} + \beta)^l$，
非イベント期間では $p_{\text{base}}^l$ である．
$r = (p_{\text{base}} + \beta) / p_{\text{base}} > 1$ とおくと，
$\text{mag}(P^*) \propto p_{\text{base}}^l (r^l - 1)$ である．
副次パターン $P'$ は $s$ 個のブーストアイテムと $l - s$ 個の非ブーストアイテムを持つため，
$\text{mag}(P') \propto p_{\text{base}}^l (r^s - 1)$ となる．
$r > 1$ かつ $l > s$ より $r^l > r^s$ であるから，
$\text{mag}(P^*) > \text{mag}(P')$ が任意のパターン長 $l$ で成立する．
なお，この結果は独立出現の仮定に依存しており，
アイテム間の強い相関がある場合には順序が逆転しうる．
EX4（Correlated条件）の精度低下はこの限界を反映している．
\end{proof}

重複排除ステップの計算量はイベントあたり $O(n \cdot k)$ であり，
$n$ は有意帰属数，$k$ はパターン最大長である．
置換検定ステップに比して無視可能なコストである．

\subsection{計算量分析}

パイプライン全体の時間計算量は以下の通りである:
\begin{itemize}
\item \textbf{Step 1}（変化点抽出＋局所サポート計算）: $O(|\mathcal{F}| \cdot \bar{C} \cdot h \cdot \log n)$，$|\mathcal{F}|$ は共起パターン数（$|P| \geq 2$），$\bar{C}$ は平均密集区間数，$h$ はレベルウィンドウ幅，$n$ は平均パターン出現数
\item \textbf{Step 2}（変化量評価）: Step 1 に含まれる（追加走査なし）
\item \textbf{Step 3--4}（帰属＋置換検定）: $O(|\mathcal{F}'| \cdot |\mathcal{E}| \cdot B \cdot C)$，
$|\mathcal{F}'|$ はフィルタ後パターン数，$B$ は置換回数，$C$ は平均変化点数
\item \textbf{Step 5}（重複排除）: $O(R \cdot k)$，$R$ は有意結果数
\end{itemize}

サポート振幅フィルタが $|\mathcal{F}'|$ を90\%以上削減するため，
大規模パターン集合に対してもStep 3--4は効率的に動作する．
