\section{Supplementary Material}
\label{sec:supplement}

本補遺では，本文で省略した実験条件・パラメータ設定，
およびデータ形式と Ground Truth 判定の定義を補足する．

\subsection{実験条件とパラメータ設定}
\label{sec:supp_exp_conditions}

本文の A1/A2/A4 は，すべて Poisson 生成モデルに基づく合成データで評価した．
トランザクション $t$ に対して，バスケット数とバスケット内アイテム数を
それぞれ次で生成する．
\[
N_t = \max\{1, \mathrm{Poisson}(\lambda_{\text{baskets}})\}.
\]
\[
K_{t,b} = \max\{1, \mathrm{Poisson}(\lambda_{\text{basket-size}})\}.
\]
カテゴリ集合は
\[
C_g = \{\,i \mid i \bmod G = g\,\}
\]
で定義し，各バスケットでは主カテゴリ $g$ を一様選択した後，
確率 $p_{\text{same}}$ で $C_g$ から，確率 $1-p_{\text{same}}$ で全アイテム集合から
Zipf 重み $w_i \propto (i+1)^{-\alpha_{\text{zipf}}}$ に従ってアイテムを抽出する．

\begin{table}[t]
\caption{固定パラメータ（A1/A2/A4 共通）}
\label{tab:supp_fixed_params}
\centering
\begin{tabular}{ll}
\toprule
項目 & 値 \\
\midrule
生成モデル & Poisson \\
$n_{\text{items}}$ & 200 \\
$\lambda_{\text{basket-size}}$ & 3.0 \\
$p_{\text{same}}$ & 0.8 \\
$\alpha_{\text{zipf}}$ & 1.2 \\
\texttt{window\_size} & 500 \\
\texttt{min\_support} & 50 \\
\texttt{max\_length} & 4 \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[t]
\caption{実験ごとの sweep 設定と seed 数}
\label{tab:supp_sweep}
\centering
\begin{tabular}{llll}
\toprule
実験 & sweep 軸 & sweep 値 & seed \\
\midrule
A1 & $\lambda_{\text{baskets}}$ & $\{1.0, 1.5, 2.0, 3.0, 5.0\}$ & $0..4$（5本） \\
A2 & $G$ & $\{3, 5, 10, 20, 50\}$ & $0..4$（5本） \\
A4 & $N_{\text{transactions}}$ & $\{10^3, 10^4, 10^5, 10^6\}$ & $0..2$（3本） \\
\bottomrule
\end{tabular}
\end{table}

A1 の図（本文 Fig.~\ref{fig:a1_time_all_methods}--\ref{fig:a1_multi_all_methods}）で用いた系列は，
\texttt{phase1}, \texttt{apriori\_window\_flatten}, \texttt{apriori},
\texttt{fp\_growth}, \texttt{eclat}, \texttt{lcm}, \texttt{pfpm},
\texttt{ppfpm\_gpf\_growth}, \texttt{lpfim}, \texttt{lppm} である．
なお実験出力 CSV ではスケーラビリティ sweep を実装都合で \texttt{A3-P} と命名しているが，
本文では実験番号体系に合わせて A4 と表記した．

\subsection{データ形式と乱数設定}
\label{sec:supp_data_format}

生成データ（\texttt{*\_seedX.txt}）は 1 行 1 トランザクション形式で，
同一行内のバスケットを区切り文字 \texttt{" | "} で連結する．
例: \texttt{1 4 9 | 2 8}

乱数は Python の \texttt{random.Random(seed)} を用い，
seed は A1/A2 で \texttt{0..4}，A4 で \texttt{0..2} を使用する．
したがって，同一 seed・同一パラメータ・同一 Python 実行系で
トランザクションファイルは決定的に再生成される．

\subsection{Ground Truth の再計算}
\label{sec:supp_gt}

GT 判定は \texttt{experiments/gen\_synthetic.py} の
\texttt{compute\_ground\_truth()} を用いて実施する．
多項目集合 $S$（$2 \leq |S| \leq max\_length$）に対して，
\begin{itemize}
    \item \texttt{true\_support(S)}: 同一バスケット内で $S$ が成立した回数
    \item \texttt{txn\_support(S)}: トランザクション単位（バスケット横断可）で $S$ が成立した回数
\end{itemize}
を計数し，
\texttt{true\_support(S) < min\_support <= txn\_support(S)}
を満たす場合を spurious と判定する．
SPR は本文 Eq.~(\ref{eq:spr}) に対応し，
分母を \texttt{txn\_support >= min\_support} の多項目集合数とした比率として計算する．
