# Pending Questions

## Resolved
- Q1: Should anti-dense intervals use strict inequality (< theta) or non-strict (<= theta)?
  - Decision: Use strict inequality (< theta) for symmetry with dense (>= theta).
- Q2: How to handle boundary between dense and anti-dense (exactly at threshold)?
  - Decision: At threshold counts as dense (>= theta), consistent with existing implementation.

## Open
- None currently.
