# corrections.md — 実装修正ログ

AIへの修正指示を構造化して記録し、同じミスの再発を防ぐ。

---

## 運用ルール

1. **記録タイミング**: ユーザーから修正指示を受けた直後に追記する
2. **根本原因を書く**: 「何を直したか」だけでなく「なぜ間違えたか」まで掘り下げる
3. **昇格基準**: 同タグのエントリが **3件** に達したら、内容を抽象化して `CLAUDE.md` のルールに昇格させ、エントリに `昇格: 済` を付ける
4. **コード修正の場合**: 対応するテストを追加してリグレッションを防ぐ

---

## タグ一覧

| タグ | 対象 |
|------|------|
| `#git` | git 操作・コミット・ブランチ管理 |
| `#gitignore` | .gitignore のスコープ・パターン |
| `#path` | ファイルパス・ディレクトリ名 |
| `#rust` | Rust 実装・型・借用など |
| `#python` | Python 実装・テスト |
| `#algorithm` | アルゴリズム・ロジック |
| `#docs` | ドキュメント・README・コメント |
| `#config` | 設定ファイル・JSON・TOML |
| `#naming` | 命名・用語の統一 |
| `#scope` | 変更範囲の過不足（多すぎ・少なすぎ） |

---

## ログ

<!-- 新しいエントリは末尾に追加する -->

### 2026-03-02 — gitignore で `target/` をルート固定しなかった

- **修正指示**: `paper/target/` がビルド成果物の除外パターン `target/` にマッチしてしまい、論文ターゲットディレクトリを git add できなかった
- **誤った実装**: `.gitignore` に `target/` と記述（サブディレクトリにもマッチ）
- **正しい実装**: `/target/` と記述してリポジトリルートにアンカー固定
- **根本原因**: gitignore のプレフィックスアンカリング（先頭の `/` の意味）を失念していた
- **昇格ルール**: —
- **タグ**: `#git` `#gitignore`
- **昇格**: 未

---

### 2026-03-02 — CLAUDE.md のブランチ名が古いまま残っていた

- **修正指示**: §5 Git Workflow の作業ブランチが `dev_new_model` と記載されていたが実際は `dev_takayasu`
- **誤った実装**: リポジトリ作成時のデフォルト値を確認せずにドキュメント化した
- **正しい実装**: `git branch --show-current` で実ブランチ名を確認してから記載する
- **根本原因**: ドキュメントを書く際に実態確認をせず記憶・推測で書いた
- **昇格ルール**: —
- **タグ**: `#docs` `#git`
- **昇格**: 未

